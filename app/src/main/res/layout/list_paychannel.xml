<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/payChannel_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@color/white"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/imageView_payChannel"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_visa_logo" />

    <TextView
        android:id="@+id/textView_payChannel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:padding="8dp"
        android:text="Cash"
        android:textColor="@color/black"
        android:textSize="14sp"
        android:textStyle="normal" />

    <ImageView
        android:id="@+id/imageView_paySelected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:foregroundTint="@color/accentColor"
        android:src="@drawable/ic_check"
        android:visibility="gone"
        app:tint="@color/accentColor" />
</LinearLayout>